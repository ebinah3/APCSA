package nnew;
import java.util.Scanner;

public class March2 {
	
	public static boolean isGoofy(int num){
		//int dig = 0;
		int sum=0;
		for(int i = 0; Math.pow(10, i) <= num; i++){
			int div = num - (int)Math.pow(10, i);
			if(div != 0 && num%div == 0){
				sum+= div;
			}
			//dig = i;
		}
		if (sum%2 == 1){
			return true;
		}
		return false;
	}
	
	public static int[] getSomeGoofyNumbers(int count)
	{
		int[] result = new int[count];
		int counter = 0;
		for(int i = 1; ;i++){
			if(isGoofy(i)){
				result[counter] = i;
				counter++;
				if(counter == count){
					break;
				}
			}
		}
		return result;
	}
	public static void main(String[] args){
		String output = "";
		System.out.print(isGoofy(12));
		System.out.print(Math.pow(10, 2));
		Scanner s = new Scanner(System.in);
		for(int i = 0; i<getSomeGoofyNumbers(s.nextInt()).length; i++ ){
			output = output + " " + getSomeGoofyNumbers(s.nextInt())[i];
		}
		System.out.println(output);
	}
}
